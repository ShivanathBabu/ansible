- name: install packages
  hosts: frontend
  tasks:
    - name: installing required packages
      ansible.builtin.packages:
        name: "{{item}}"
        state: installed
      loop:
      - mysql
      - zip
      - nginx