- name: install packages
  hosts: frontend
  become: yes
  tasks:
    - name: installing required packages
      ansible.builtin.package:
        name: "{{item}}"
        state: present
      loop:
      - mysql
      - zip
      - nginx