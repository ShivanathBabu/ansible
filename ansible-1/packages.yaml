- name: install packages
  hosts: frontend
  tasks:
    - name: installing required packages
      ansible.builtin.packages:
        name: "{{items}}"
        state: installed
      loop:
      - mysql
      - zip
      - nginx