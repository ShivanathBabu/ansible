- name: Install nginx
  hosts: frontend
  tasks:
    - name: Installing nginx
      ansible.builtin.dnf:
        name: nginx
        state: present
    - name: start nginx
      ansible.builtin.service:
        name: nginx
        state: started
        enabled: yes
