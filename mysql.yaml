- name: install mysql
  hosts: frontend
  become: yes
  tasks:
    - name: installing database
      ansible.builtin.dnf:
        name: mysql-server
        state: present
    
    - name: start and enable service
      ansible.builtin.service:
        name: mysqld
        state: started
        enabled: yes